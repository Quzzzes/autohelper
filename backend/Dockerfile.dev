FROM node:20-alpine

WORKDIR /app

# Устанавливаем зависимости отдельным слоем — кэшируется Docker
COPY package.json .
RUN npm install

# Код монтируется через volume в docker-compose — не копируем здесь
EXPOSE 3000

CMD ["npm", "run", "dev"]
